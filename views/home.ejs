<!DOCTYPE html>
<html lang="en" class="h-full w-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-800 h-full w-full text-white flex items-center justify-center flex-col">
    <% if (message) { %>
        <div class="alreadyExists bg-red-600 opacity-50 w-[100%] border-2 border-red-300 flex justify-center text-2xl items-center h-[10%] absolute top-0">
            User Already Exists
        </div>
    <% } %>
    <h1 class="text-4xl uppercase mb-5">Signup Form</h1>
    <div class="form">
        <form action="/user/create" method="post" class="border-2 border-zinc-600 rounded-md p-10 flex flex-col ">
            <label for="username">Enter Username:</label>
            <input type="text" required name="username" id="username" placeholder="username" class="mb-5 border-2 border-zinc-600 rounded-md px-3 py-4">
            <label for="email">Enter email:</label>
            <input type="email" required name="email" id="email" placeholder="email" class="mb-5 border-2 border-zinc-600 rounded-md px-3 py-4">
            <label for="password">Enter password:</label>
            <input type="password" required name="password" id="password" placeholder="password" class="mb-5 border-2 border-zinc-600 rounded-md px-3 py-4">
            <input type="submit" value="Create User" class="mb-5   py-4 bg-blue-500 rounded-md mt-5">
            <p class="text-xl"> 
                Already a user click <a href="/user/login" class="text-blue-500">here</a> to login
            </p>
        </form>

    </div>
</body>
</html>