<!DOCTYPE html>
<html lang="en" class="h-full w-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-800 h-full w-full text-white flex items-center justify-center flex-col">
    <% if (message) { %>
        <div class="alreadyExists bg-red-600 opacity-50 w-[100%] border-2 border-red-300 flex justify-center text-2xl items-center h-[10%] absolute top-0">
            Email or password is incorrect
        </div>
    <% } %>
    <% if (userDoesNotExist) { %>
        <div class="alreadyExists bg-red-600 opacity-50 w-[100%] border-2 border-red-300 flex justify-center text-2xl items-center h-[10%] absolute top-0">
           User Does not exist Please Create one
        </div>
    <% } %>
    <% if (loginFirst) { %>
        <div class="alreadyExists bg-red-600 opacity-50 w-[100%] border-2 border-red-300 flex justify-center text-2xl items-center h-[10%] absolute top-0">
           You need to login first
        </div>
    <% } %>
        <h1 class="text-4xl uppercase mb-5">Login Form</h1>
    <div class="form">
        <form action="/user/login" method="post" class="border-2 border-zinc-600 rounded-md p-10 flex flex-col ">
           
            <label for="email">Enter email:</label>
            <input type="email" name="email" id="email" placeholder="email" required class="mb-5 border-2 border-zinc-600 rounded-md px-3 py-4">
            <label for="password">Enter password:</label>
            <input type="password" name="password" id="password" placeholder="password" required class="mb-5 border-2 border-zinc-600 rounded-md px-3 py-4">
            <input type="submit" value="Log In" class="mb-5   py-4 bg-blue-500 rounded-md mt-5">
           
            <p class="text-center text-2xl">Create <a href="/user/create" class="text-blue-500">Account </a> </p>
            <p class="text-center text-2xl text-blue-500"><a href="/admin/login">Admin Login</a> </p>
        </form> 
    </div>
</body>
</html>