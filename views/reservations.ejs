<!DOCTYPE html>
<html lang="en" class="w-full h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="w-full h-full bg-zinc-700 flex justify-center items-center text-white flex-col">
    <div class="absolute left-0 top-3">
     <a href="/reservation/restaurants" class="bg-blue-500 px-3 py-3 rounded md">Go To Restaurants</a>
    </div>
    <% if (reservations && reservations.length>0) { %>
        <div class="reservations w-[80%] h-[90%] border-2 border-zinc-500 rounded-md p-4">
     <% reservations.forEach((data) => { %>
            <div class="reservation border-2 border-white pl-10 flex items-center justify-between mb-5">
                <div class="info">
                    <p class="text-2xl mb-3">
                        Name:<%=data.restaurantId.name%>
                    </p>
                    <p class="text-2xl mb-3">Date:<%=data.date%></p>
                    <p class="text-2xl mb-3">
                        Time:<%=data.time%>
                    </p>
                </div>
                <div class="cancel pr-10">
                    <form action="/reservation/reservations/<%=data._id%>" method="post">
                        <input type="submit" value="Cancel booking" class="bg-red-600 px-3 py-3 rounded-md text-2xl cursor-pointer">
                    </form>
                </div>
                
            </div>
            <% }) %>
        </div>
    <% }else{ %>
        <div>
            <h1>No reservations found</h1>
        </div>
        <%}%>
</body>
</html>